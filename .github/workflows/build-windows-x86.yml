name: Build for Windows x86

on:
  push:
    branches:
      - build-test
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'

    - name: Install Dependencies
      run: |
        choco install cmake ninja --install-arguments="'/InstallDir:C:\tools\cmake'"

    - name: Install Skia
      run: |
        wget https://github.com/aseprite/skia/releases/download/m102-861e4743af/Skia-Windows-Release-x86.zip
        unzip Skia-Windows-Release-x86.zip -d skia

    - name: Configure CMake
      run: |
        cmake -S . -B build -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DLAF_BACKEND=skia \
          -DSKIA_DIR=skia \
          -DSKIA_LIBRARY_DIR=skia/out/Release-x86 \
          -DENABLE_UI=ON

    - name: Build
      run: |
        cmake --build build --target aseprite --config Release

    - name: Package Application
      run: |
        mkdir -p dist
        cd build
        cp -r bin aseprite_windows
        zip -r ../dist/aseprite-windows-x86.zip aseprite_windows

    - name: Upload Artifact
      uses: actions/upload-artifact@v3
      with:
        name: aseprite-windows-x86
        path: dist/aseprite-windows-x86.zip
        retention-days: 5
